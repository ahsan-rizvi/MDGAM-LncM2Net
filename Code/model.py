import torch
import torch.nn as nn
import torch.nn.functional as F
import dgl
from dgl.nn import GATConv


class LSTMEncoder(nn.Module):

class net(nn.Module):

class MDGAMLncM2Net(nn.Module):
    RNA encoders
    GAT layer for attention
    Output projection
    Encode RNA sequences
    Concatenate all node embeddings
    Apply GAT over the graph
    If multiple heads, concatenate or average
    Apply BiLSTM layer 
    Final projection
